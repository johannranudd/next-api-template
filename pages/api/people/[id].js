// import { people } from '../../../data/people';

// export default function handler(req, res) {
//   const peopleID = people.filter((item) => item.id === Number(req.query.id));
//   res.status(200).json(peopleID);
// }
